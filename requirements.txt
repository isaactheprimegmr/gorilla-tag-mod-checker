# Gorilla Tag Mod Checker - Requirements
# Python 3.7+

# Core dependencies
# (Standard library only - no external dependencies required)

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
black>=22.0.0
flake8>=4.0.0
pylint>=2.15.0
mypy>=0.990

# Optional dependencies for CLI enhancement
click>=8.0.0
colorama>=0.4.5
tabulate>=0.9.0

# Optional dependencies for web interface
flask>=2.2.0
flask-cors>=3.0.0
